# Docker Compose environment file for myUplink2mqtt
# Copy this file to .env and customize for your environment
# These values can be overridden by setting environment variables

# myUplink API Configuration
# Obtain from https://dev.myuplink.com/
# MYUPLINK_CLIENT_ID=your_client_id_here
# MYUPLINK_CLIENT_SECRET=your_client_secret_here

# MQTT Broker Configuration
# If using the included Mosquitto service in docker-compose.yml, use 'mosquitto' as hostname
# If using external MQTT broker, use its IP/hostname and port
MQTT_BROKER_HOST=mosquitto
MQTT_BROKER_PORT=1883
# MQTT_USERNAME=mqtt_user          # Optional: only if your MQTT broker requires authentication
# MQTT_PASSWORD=mqtt_password      # Optional: only if your MQTT broker requires authentication
MQTT_BASE_TOPIC=myuplink

# Home Assistant Configuration
HA_DISCOVERY_PREFIX=homeassistant

# Polling Configuration
# Interval in seconds between API polls (default: 300 = 5 minutes)
POLL_INTERVAL=300

# Logging Configuration
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
